<nav>
    <ul>
        {%- for post in collections.menu -%}
        <li><a {% if page.url === post.url %} aria-current="page"{% endif %} href="{{ post.url }}">{{ post.data.title }}</a></li>
        {%- endfor -%}
    </ul>
</nav>